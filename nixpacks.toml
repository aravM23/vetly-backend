# Pin Python so Railway's Nixpacks matches what we tested locally.
[phases.setup]
nixPkgs = ["python312", "gcc"]

[phases.install]
cmds = ["pip install --upgrade pip && pip install -r requirements.txt"]
